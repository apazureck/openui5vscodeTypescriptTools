{
    "ui5tsController": {
        "description": "Inserts a new UI5 typescript controller",
        "prefix": "ui5controller",
        "body": ["sap.ui.define([\"sap/ui/core/mvc/Controller\",",
            "// Insert your used ui5 elements here to get them loaded",
            "], function (Controller: sap.ui.core.mvc.Controller) {",
            "    \"use strict\";",
            "    return Controller.extend(\"${namespace}.controller.${TM_FILENAME_BASE/(.*)\\..+$/$1/}\");",
            "});",
            "",
            "namespace ${namespace} {",
            "",
            "    export class ${name} extends sap.ui.core.mvc.Controller {",
            "       onInit(): void {",
            "           console.info(\"${TM_FILENAME_BASE/(.*)\\..+$/$1/} initialized\");",
            "       }",
            "       onBeforeRendering(): void {",
            "       }",
            "       onAfterRendering(): void {",
            "       }",
            "       onExit(): void {",
            "       }",
            "    }",
            "}"
        ]
    }
}